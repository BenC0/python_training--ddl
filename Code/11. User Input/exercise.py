# ------------------------------------------------------------------------------
# Exercise
# ------------------------------------------------------------------------------

# Exercise
# 1. Using the `input()` function, get values for `a` and `b` from the user,
# pass the values to the `sum()` function and print the answer.

def sum(a,b):
	return a + b